import React from "react";

const Topbar = () => {
  return (
    <nav className="w-full bg-white p-5 shadow-sm">
      <label htmlFor="sidebar" className="drawer-button">
        Open drawer
      </label>
    </nav>
  );
};

export default Topbar;
