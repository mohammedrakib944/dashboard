import Loader from "@/components/Loader/Loading";
import React from "react";

const Loading = () => {
  return (
    <div>
      <Loader title="Page loading..." />
    </div>
  );
};

export default Loading;
